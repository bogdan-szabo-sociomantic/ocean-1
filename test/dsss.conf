name=unittest

[main.d]
target=         unittest
buildflags=     -Isrc -w -gc \
                -debug=OceanUnitTest -debug -unittest \
                -L/usr/lib/libtokyocabinet.so\
                -L/usr/lib/minilzo.a \
                -d -L-ltango32

